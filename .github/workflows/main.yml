name: CI/CD Pipeline

# This workflow is disabled for local builds
# Use the build-and-push.sh script for local Docker operations

on:
  workflow_dispatch:  # Manual trigger only
  # push:
  #   branches:
  #     - main

jobs:
  info:
    name: Build Information
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show build info
        run: |
          echo "This workflow is disabled for local builds."
          echo "Use the following commands to build and push locally:"
          echo "  npm run dc:build:push"
          echo "  or"
          echo "  ./build-and-push.sh"
